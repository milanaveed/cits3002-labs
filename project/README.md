# Battleship
Mila Zhang (22756463)

## Instructions
After changing into the project directory, simply run `python server.py` to establish the server. Then create new terminal, change into the project directory and run `python client.py` to create a new client.